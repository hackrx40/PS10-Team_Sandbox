

## Installation

```
pip install flask python-dotenv langchain huggingface-hub faiss-cpu
pip install sentence_transformers
put api key in .env

pip install transformers
pip install tensorflow 
upgrade pip

npm install pdftron/webviewer

pip install cors
pip install flask_cors
```
